#include<bits/stdc++.h>
using namespace std ; 

int main(){
	string s; cin >> s ;
	int n = s.size() ; 
	vector<char > st ; 
	for(int i=0 ; i<n ; i++ ){
		if(st.empty()) 
			st.push_back(s[i]) ; 
		else{
			if(s[i] == st.back() ){
				st.pop_back() ; 
			}
			else st.push_back(s[i]) ; 
		}
	}
	if(st.empty()) cout <<"EMPTY" ; 
	else {
		for(char x : st ) cout << x ; 
	}
}
